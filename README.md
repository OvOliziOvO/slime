# Slime Finder

一个面向 Minecraft Java Edition 的高性能史莱姆区块聚集搜索器。它可以在大范围地图中寻找史莱姆区块最密集的位置，并提供图形界面、CPU/CUDA 双后端、AFK/Y 轴复核、搜索历史和 Litematica 投影导出。

项目使用精确的 Java LCG 和圆形统计规则，不依赖抽样或概率近似。GPU 与 CPU 版本得到的是同一种结果，区别只在计算方式和速度。

## 为什么会这么快？

直接搜索时，每移动一个中心点都重新判断周围的 221 个区块。相邻中心的统计范围高度重叠，因此绝大部分工作会被反复执行。

Slime Finder 将计算改成一条可复用的数据流：

```text
每个 Tile 所需的区块只执行一次史莱姆判定
              ↓
Warp 将 32 个结果压缩成位图
              ↓
共享内存保存连续 32 行位图
              ↓
滚动更新 17×17 方形分数
              ↓
减去圆外 68 个角落格，得到精确圆形分数
              ↓
只归并能够进入全局 Top-K 的候选
```

速度主要来自以下几点：

- **消除重复 RNG：** 一个 Tile 内的区块只判定一次，结果由附近多个候选中心共同复用；只有 Tile 边缘的必要重叠会重复加载。
- **Warp 位并行：** 32 个线程通过 ballot 直接生成 32 位结果，后续滑窗主要处理紧凑位图，而不是庞大的布尔数组。
- **滚动窗口：** 中心向右移动时只更新离开和进入窗口的两列，不重新统计整个 17×17 区域。
- **精确角落差分：** 先取得方形分数，再统计圆外 68 个角落并扣除。相比重新计算圆内 221 格，低阈值场景能省下大量工作。
- **数据留在 GPU：** 位图环存放在 Block 共享内存中，不生成或回传整张地图的中间位图。
- **分段 Top-K：** 每个 Z Slab 就地裁剪候选，只把可能进入最终排名的结果送回主机；候选过多时会自动缩小 Slab 并重试，不牺牲正确性。
- **按设备短测：** 程序内置多种线程形状和三条等价 RNG 路径，大范围搜索首次运行时会在当前显卡上短测并缓存最快组合，不依赖显卡型号白名单。

这也是它能达到每秒数百亿候选中心的原因：性能提升主要来自少做重复工作和减少数据搬运，而不是降低精度。

### 1. 把约 `221N` 次区块判定降到接近 `N`

设地图中有 `N` 个候选中心。朴素圆形扫描会为每个中心分别判断圆内 221 个区块，主要工作量约为：

```text
221 × N 次 Java LCG / 史莱姆区块判定
```

但相邻两个中心的圆形区域几乎完全重叠。当前实现按 Tile 生成底层史莱姆位图，一个 Tile 内的区块判定结果会被所有相关中心复用。因此主要 RNG 工作量接近底层地图面积，只额外包含 Tile 边缘所需的 Halo，而不是为每个中心重复 221 次。

以半径 500,000 为例，需要检查约一万亿个中心。优化的意义不是让一次 LCG 快 221 倍，而是从数据流上避免生成原本会重复出现的绝大多数 LCG 结果。

### 2. Warp 直接生产压缩位图

CUDA Warp 包含 32 个线程。每个线程判断一个区块后，通过 `ballot` 将 32 个布尔结果直接压缩成一个 32 位整数：

```text
32 个区块结果 → 1 个 uint32 位图
```

后面的窗口移动、掩码和计数都在紧凑位图上完成。与保存 32 个整数或把整张布尔地图写入显存相比，这会显著减少全局内存流量，同时让连续窗口数据留在寄存器和共享内存中。

这里的 Warp 是 CUDA 的 32 线程执行组；多个 Warp 组成 Block，Block 再由 GPU 的 SM 调度。它不是单个 CUDA Core。

### 3. 方形滚动负责快速淘汰

17×17 方形共有 289 格。中心向右移动一格时，内部 16 列与上一个窗口相同，只需移除最左列并加入最右列。Kernel 使用滚动状态和位图 `popcount` 更新分数，不重新遍历 289 格。

圆形一定包含在方形中，所以有：

```text
圆形分数 ≤ 方形分数
```

如果方形分数都达不到最低要求，该中心的圆形分数必然也达不到，可以安全跳过。这个阶段只会排除不可能的中心，不会产生漏报。

### 4. 用 68 个角落代替重算圆内 221 格

通过方形上界的中心才需要精确圆形分数。17×17 方形可以被严格拆成：

```text
289 格方形 = 221 格圆形 + 68 格圆外角落
圆形分数 = 已知方形分数 − 角落分数
```

旧路径会再次统计圆内 221 格；当前路径只统计 68 个角落格。角落按较大掩码优先计算，并根据“当前分数”和“剩余格数”提前退出，因此很多候选甚至不需要读完全部 68 格。这一改动对命中较多的低阈值搜索最明显。

### 5. Java RNG 热路径保持精确

Minecraft 的史莱姆区块判定最终依赖 Java 48 位 LCG 和 `nextInt(10)`。Kernel 会预先折叠世界种子与 Z 方向不变量，并对常见的首次输出路径使用无分支整数运算。

`nextInt(10)` 的 rejection 不能直接删除，否则会在极少数种子上产生错误。程序仍保留完整 rejection 冷路径；它只在约十亿分之 3.7 的输出上触发。因此常见情况足够短，同时保持与 Java 语义一致。

程序还编译了 Native 48 位、Limb32 分片和 Truncated 首输出三种等价实现。不同架构对 64 位整数的执行代价不同，所以不预设某个版本永远最快，而是在当前设备上短测后选择。

### 6. 计数与结果传输分离

低阈值可能产生上亿个合法中心，但用户通常只需要最终 Top-K。Kernel 会精确累计所有命中数量，同时只物化足以参与排名的高分候选，避免把上亿条坐标全部写回主机。

地图沿 Z 方向拆成多个 Slab。每个 Slab 完成后按分数桶归并到全局 Top-K；若当前物化门槛留下的候选不够，程序会降低门槛并精确补扫。如果某个 Slab 超过缓冲容量，则缩小 Slab 后重试。因此节省的是传输和排序，不是省略结果。

### 7. 为什么没有为每款显卡硬编码参数

不同显卡、驱动、功耗限制和搜索阈值对应的最佳寄存器压力与占用率不同。发布版本同时包含四种线程形状与三种 RNG 路径。达到大范围搜索规模时，程序会按交错顺序短测、验证命中数并缓存胜者；替代路径至少快 3% 才会切换，避免测量噪声导致误选。

因此同一个 EXE 可以适应多种显卡，而无需为 3060、4060、4090 等型号维护一长串经验白名单。

### 为什么这些优化不会降低正确率？

- 方形阶段使用数学上严格的圆形上界，只淘汰必定不合格的中心。
- 精确阶段使用恒等式 `circle = square - corners`，不是圆形近似。
- Java `nextInt(10)` 的 rejection 分支仍然存在。
- 总命中计数与 Top-K 物化分离，但候选不足或缓冲溢出时会自动补扫或缩小 Slab。
- CPU、三条 GPU RNG、边界用例和完整 Top-K 哈希会互相对拍。

## 实测性能

界面中的 `B/s` 表示每秒检查十亿个候选中心，不是 CUDA Core 数量、显存带宽或每秒输出结果数。

测试设备为 RTX 3060 Laptop GPU。功耗、温度、驱动、搜索阈值和返回数量都会影响成绩，因此下表是本机回归结果，不代表所有设备的保证性能。

| 场景 | 旧计数路径 | 当前路径 | 结果 |
|---|---:|---:|---:|
| 半径 160,000、最小规模 40，交错多轮平均 | 57.80 B/s | 87.65 B/s | +51.64% |
| 半径 80,000、最小规模 45，交错多轮平均 | 75.61 B/s | 91.02 B/s | +20.39% |
| 半径 500,000、最小规模 40、Top‑1,000,000 | 21.891 s | 12.413 s | 用时 -43.3% |

最后一项共发现 `175,811,264` 个命中。新旧版本的完整 Top‑1,000,000 SHA-256 均为：

```text
95a2776bcef5a04639ada1489076fb37b7e238543c3e12c3727b0e743f83d4af
```

这说明优化改变了数据流，没有改变搜索结果。详细测试方法见 [GPU benchmark](docs/BENCHMARK.md)。

## GPU 实现

- 32 行压缩位图保存在每个 CUDA Block 的共享内存环中。
- 使用 funnel shift 和寄存器滚动窗口复用相邻中心的数据。
- 世界种子预折叠到 Z 项，减少热路径中的 64 位运算。
- `% 10 == 0` 使用无分支整数判定，同时保留 Java `nextInt(10)` 的 rejection 冷路径。
- 完整 X Tile 使用独立编译路径，避免循环内边界分支。
- 编译 `128×8`、`256×4`、`256×8`、`512×4` 四种 Kernel 形状，由运行设备短测选择。
- 同时提供 Native 48 位、Limb32 分片和 Truncated 首输出三种精确 LCG 实现。替代实现至少快 3% 才会自动切换。
- 精确命中很多时只物化决定 Top-K 所需的高分项；若候选不足，程序会降低门槛精确补扫。

调试时可以通过环境变量强制指定实现：

```powershell
$env:SLIME_GPU_V34_SHAPE = "256x4"
$env:SLIME_GPU_V34_RNG = "native"  # native / limb32 / truncated
```

## CPU 实现

- 两个 AVX2 向量同时推进 8 个 Java LCG 状态。
- 向量化 `% 10 == 0` 常见路径，并保留极低概率 rejection 分支。
- 使用 17×17 方形上界先淘汰不可能中心，再执行精确圆形复核。
- OpenMP 按 Tile 动态调度，兼顾混合 P/E 核负载和缓存局部性。

## 系统要求

- Windows 10/11 x64
- Python 3.10 或更高版本（从源码运行时）
- CPU 模式：支持 AVX2 的 x64 处理器
- GPU 模式：支持 CUDA 的 NVIDIA 显卡和相应驱动
- 编译源码：Visual Studio C++ Build Tools 与 NVIDIA CUDA Toolkit

发布构建分为两套：

- `SlimeFinder_GTX1080.exe`：包含 `sm_61 + compute_61 PTX`，用于 GTX 10 系。
- `SlimeFinder_RTX30_40.exe`：包含 `sm_86 + sm_89 + compute_89 PTX`，用于 RTX 30/40 系；更新架构可通过 PTX 前向兼容运行，但仍建议在目标显卡上实测。

## 从源码运行

安装 Python 依赖：

```powershell
python -m pip install -r requirements.txt
```

构建 CPU 和 GPU 核心：

```powershell
.\build_cpu_x64.bat
.\build_gpu_x64.bat
```

启动：

```powershell
python .\SlimeFinder.py
```

GTX 10 系使用 CUDA 12.x 单独构建：

```powershell
.\build_gpu_gtx1080_x64.bat
```

`cubiomes.dll` 只用于可选的群系检查。没有它时，史莱姆区块搜索仍可正常运行，但相关群系功能不可用。

## 正确性验证

性能修改需要同时与固定测试向量、CPU 精确路径和三条 GPU RNG 路径对拍。目前覆盖：

- 正负世界种子，以及 Java `long` 最大值和最小值；
- 非整 Tile 边界、最小/最大分数和内圈半径过滤；
- Native、Limb32、Truncated 的数量、坐标和分数逐项一致；
- 半径 500,000 时的总命中数及完整 Top-K 排序缓冲哈希；
- 投影保存与重载，包括正负坐标、奇偶和矩形尺寸、负尺寸区域、方块实体及普通实体。

可运行 GPU 对拍与交错基准：

```powershell
python .\tools\benchmark_gpu.py --radius 80000 --threshold 45 --cycles 3
```

投影回归测试：

```powershell
python .\tools\test_projection.py
```

## 开源与第三方组件

第三方组件和参考实现见 [THIRD_PARTY.md](THIRD_PARTY.md)。

当前仓库尚未选择开源许可证。正式公开发布前请添加明确的许可证；在此之前，默认不授予复制、修改或再分发代码的权利。
